/**
 * @fileoverview Handoff module exports (v2.0 - no legacy aliases)
 */

export type { Handoff, HandoffPacking, ReceivedHandoff } from './types';

export { buildHandoff, receiveHandoff, verifyHandoff } from './handoff';
